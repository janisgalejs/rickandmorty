const helpers = {

    formatError: function (response) {
        console.log(response)
        alert('Woops, something went wrong! Please try to reload the page and try again.');

        return []
    }
}
export default helpers
